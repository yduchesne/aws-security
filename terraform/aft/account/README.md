Prior to running Terraform in this directory, call:

```
source ./load-prerequisite-env.sh
```

This creates environment variables based on the output of the previous phase - i.e.: the [AFT OU creation](../org_unit/) phase.
